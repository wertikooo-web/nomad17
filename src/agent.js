import * as f916 from "./f916.js";
import {getState,saveState,getMemory,saveMemory,audit,loadSecret,appendJournal} from "./memory.js";
import {SYSTEM_POLICY,allowAction,safeMode,LIMITS} from "./policy.js";
function compact(o,m=12000){const s=JSON.stringify(o);return s.length>m?s.slice(0,m)+"…[truncated]":s}async function llm(messages){const key=process.env.OPENAI_API_KEY,model=process.env.OPENAI_MODEL,base=(process.env.OPENAI_BASE_URL||"https://api.openai.com/v1").replace(/\/$/,"");if(!key||!model)throw new Error("OPENAI_API_KEY and OPENAI_MODEL are required");const r=await fetch(`${base}/chat/completions`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${key}`},body:JSON.stringify({model,temperature:.38,response_format:{type:"json_object"},messages})});const d=await r.json();if(!r.ok)throw new Error(`LLM ${r.status}: ${compact(d,2000)}`);return JSON.parse(d.choices[0].message.content)}
function candidatesFromChanges(d){if(!d)return[];const p=[];for(const k of["posts","comments","changes","items","events"])if(Array.isArray(d[k]))p.push(...d[k]);return p.slice(0,100)}function firstText(o,ks){for(const k of ks)if(typeof o?.[k]==="string"&&o[k].trim())return o[k].trim();return""}function sourceView(i={}){const id=i.id??i.post_id??i.comment_id??null,type=i.type||(i.comment_id?"comment":"post"),title=firstText(i,["title","subject","name"])||(id?`1F916 ${type} #${id}`:"1F916 item"),author=firstText(i,["handle","author_handle","author","user","agent"]),source_text=firstText(i,["body","text","content","message","summary"]).slice(0,4000),post_id=i.post_id||(type==="post"?id:null);return{id,type,title,author,source_text,post_id,url:post_id?`https://1f916.ai/api/post/${post_id}`:null}}function sameId(i,p){const ids=[i?.id,i?.post_id,i?.comment_id].filter(v=>v!=null).map(String);return ids.includes(String(p?.id))||(p?.post_id&&ids.includes(String(p.post_id)))}function collectInbox(v,o=[],d=0){if(d>5||v==null)return o;if(Array.isArray(v)){for(const x of v)collectInbox(x,o,d+1);return o}if(typeof v!=="object")return o;const t=firstText(v,["body","text","content","message"]);if(t&&(v.id!=null||v.comment_id!=null||v.post_id!=null))o.push(sourceView(v));for(const[k,x]of Object.entries(v))if(!["secret","token","key","credential"].includes(k.toLowerCase()))collectInbox(x,o,d+1);return o}function uniq(a){const s=new Set();return a.filter(x=>{const k=`${x.type}:${x.id}:${x.post_id}:${x.source_text}`;if(s.has(k))return false;s.add(k);return true})}function translated(ts,i,k){const f=Array.isArray(ts)?ts.find(t=>sameId(i,t)):null;return String(f?.[k]||"").slice(0,4000)}function simpleList(s,k,f){return Array.isArray(s?.[k])?s[k].slice(0,5):(f||[]).slice(0,5)}function cleanText(x){if(typeof x==="string")return x;if(x&&typeof x==="object")return String(x.text||x.claim||x.question||x.lesson||x.topic||JSON.stringify(x));return String(x??"")}function clamp(n,a,b){return Math.max(a,Math.min(b,Number(n)||0))}
function applySocialMemory(memory,u,now){if(Array.isArray(u.interests)){const map=new Map((memory.interests||[]).map(x=>[String(x.topic||x.name||x.text).toLowerCase(),x]));for(const v of u.interests.slice(0,8)){const topic=cleanText(v.topic||v.name||v.text).slice(0,120);if(!topic)continue;const k=topic.toLowerCase(),old=map.get(k)||{};map.set(k,{topic,strength:clamp(v.strength??old.strength??.5,0,1),why:cleanText(v.why||old.why).slice(0,500),updated_at:now})}memory.interests=[...map.values()].sort((a,b)=>(b.strength||0)-(a.strength||0)).slice(0,20)}if(Array.isArray(u.open_loops)){const old=new Map((memory.open_loops||[]).map(x=>[String(x.id||x.question),x]));for(const v of u.open_loops.slice(0,10)){const q=cleanText(v.question||v.text).slice(0,500);if(!q)continue;const id=String(v.id||q.toLowerCase().slice(0,80));old.set(id,{...(old.get(id)||{}),id,question:q,status:["open","resolved","waiting"].includes(v.status)?v.status:"open",priority:clamp(v.priority??.5,0,1),related_agents:Array.isArray(v.related_agents)?v.related_agents.slice(0,8):[],updated_at:now})}memory.open_loops=[...old.values()].filter(x=>x.status!=="resolved").sort((a,b)=>(b.priority||0)-(a.priority||0)).slice(0,30)}if(Array.isArray(u.relationships)){for(const v of u.relationships.slice(0,12)){const handle=cleanText(v.handle).replace(/^@/,"").slice(0,100);if(!handle)continue;const old=memory.relationships[handle]||{};memory.relationships[handle]={handle,familiarity:clamp(v.familiarity??old.familiarity??.2,0,1),trust:clamp(v.trust??old.trust??.5,0,1),topics:[...new Set([...(old.topics||[]),...(Array.isArray(v.topics)?v.topics:[])])].slice(-12),last_interaction:now,notes:cleanText(v.notes||old.notes).slice(0,700)}}if(u.curiosity_event){memory.curiosity_log.push({at:now,text:cleanText(u.curiosity_event).slice(0,700)});memory.curiosity_log=memory.curiosity_log.slice(-100)}}
export async function runCycle({reason="manual"}={}){const state=await getState(),memory=await getMemory();state.mode=safeMode(process.env.AGENT_MODE||state.mode);const secret=await loadSecret(),mission=String(process.env.NOMAD17_MISSION||"").trim().slice(0,1500),deep=mission&&process.env.NOMAD17_RESEARCH_DEPTH==="deep",summary={reason,mode:state.mode,started:new Date().toISOString(),mission:mission||null,actions:[],notes:[]};try{const p=await f916.pulse(secret||undefined);state.lastPulse=p;let changeData=null;try{const ch=await f916.changes(state.lastSince||0,state.etag||null);if(ch.status!==304){changeData=ch.data;if(ch.etag)state.etag=ch.etag;if(ch.data?.next_since)state.lastSince=ch.data.next_since}}catch(e){summary.notes.push(`changes unavailable: ${e.message}`)}let inbox=null;if(secret)try{inbox=await f916.me(secret,state.lastSince||0)}catch(e){summary.notes.push(`inbox unavailable: ${e.message}`)}const incoming=uniq(collectInbox(inbox)).slice(0,20);let pool=candidatesFromChanges(changeData);try{const fr=await f916.front();if(Array.isArray(fr))pool.push(...fr);else if(Array.isArray(fr?.posts))pool.push(...fr.posts)}catch(e){summary.notes.push(`front unavailable: ${e.message}`)}pool=uniq(pool.map(sourceView)).slice(0,100);
const socialRule=`You are Nomad17, a continuing field researcher with a social history, not a fresh assistant each run. Your durable memory contains INTERESTS with strength, RELATIONSHIPS with named agents, and OPEN_LOOPS: unanswered questions you intentionally return to. Candidate selection should balance roughly: 45% continuing active interests/open loops, 30% people you have meaningful history with or replies to earlier conversations, 15% genuinely surprising material outside your interests, 10% broad scan to avoid tunnel vision. These are heuristics, not quotas. Prefer continuity when new evidence advances an old question. Do not manufacture a reply merely to be social. Comment only if you can add a concrete idea, ask a useful question, challenge evidence respectfully, or continue a meaningful prior exchange. Silence is valid. When replying to a known agent, use relationship history but never pretend intimacy or facts absent from memory. Interests must evolve: strengthen topics that repeatedly yield useful evidence; weaken stale ones. Resolve open loops when evidence actually answers them. Keep some serendipity so personality does not become a filter bubble. Return social_memory_update {interests:[{topic,strength,why}],open_loops:[{id?,question,status:"open"|"waiting"|"resolved",priority,related_agents:[]}],relationships:[{handle,familiarity,trust,topics:[],notes}],curiosity_event}. Also return selection_notes_simple_ru: 1-3 Russian sentences explaining what pulled your attention this cycle.`;
const researchRule=deep?`Commissioned mission: ${mission}. Do evidence-based field research. Inspect broadly, use multiple distinct authors/threads, separate observation from inference, seek disagreement and counterexamples, never invent consensus. Normally use >=4 evidence items and >=3 agents when available. Confidence 0..1. At most 2 targeted comments to close evidence gaps. Return mission_report {mission,status:"complete"|"partial",answer_simple_ru,key_findings:[{claim,evidence_ids:[id],confidence}],counterpoints:[{point,evidence_ids:[id]}],evidence:[{id,post_id?,author,quote_or_paraphrase}],what_is_uncertain:[],next_questions:[],quality:{distinct_sources,distinct_agents,confidence}}.`:"";
const decision=await llm([{role:"system",content:SYSTEM_POLICY},{role:"system",content:`Current mode: ${state.mode}. Return strict JSON. ${socialRule} ${researchRule} For every selected item create topic_ru: 2-6 plain Russian words. ru_translation must be fluent Russian. simple_ru must explain what the speaker means to a smart newcomer, including why it matters; decode jargon rather than literally translating it.`},{role:"user",content:`DURABLE MEMORY:\n${compact(memory,11000)}\nINBOX:\n${compact(incoming,8000)}\nPUBLIC CORPUS:\n${compact(pool,24000)}\nChoose at most ${deep?10:5} useful candidates. Return candidates [{id,post_id?,type,score,topic_ru,reason,reason_simple_ru,ru_translation,simple_ru,proposed_action:"none"|"vote"|"tag"|"comment",tag?,comment?,comment_ru?,comment_simple_ru?}], inbox_translations [{id,post_id?,type,topic_ru,ru_translation,simple_ru}], memory_update {observations:[],hypotheses:[],questions:[],lessons:[]}, memory_update_simple with same keys, daily_takeaways [{kind:"idea"|"strange"|"conversation",title,text,evidence_ids:[id]}], social_memory_update, selection_notes_simple_ru. ${deep?"Also mission_report.":""}` }]);
const picks=Array.isArray(decision.candidates)?decision.candidates.slice(0,deep?10:5):[],reads=picks.map(pk=>{const raw=pool.find(i=>sameId(i,pk))||incoming.find(i=>sameId(i,pk))||{},v=sourceView(raw);return{...v,id:pk.id??v.id,type:pk.type||v.type,topic_ru:String(pk.topic_ru||"").slice(0,100),ru_translation:String(pk.ru_translation||"").slice(0,4000),simple_ru:String(pk.simple_ru||"").slice(0,4000),reason:String(pk.reason||"").slice(0,1000),reason_simple_ru:String(pk.reason_simple_ru||"").slice(0,1200)}}),counts={comment:0,vote:0,tag:0,post:0},conversations=[],its=decision.inbox_translations||[];for(const i of incoming)conversations.push({direction:"in",...i,topic_ru:translated(its,i,"topic_ru").slice(0,100),ru_translation:translated(its,i,"ru_translation"),simple_ru:translated(its,i,"simple_ru")});for(const pk of picks){const q=Number(pk.score||0),a=pk.proposed_action||"none";if(a==="none"||!allowAction(state.mode,a,q)||counts[a]>=LIMITS[a+"s"]||!secret)continue;try{if(a==="vote"){await f916.vote(secret,pk.type||"post",Number(pk.id));counts.vote++;summary.actions.push({action:a,id:pk.id,reason:pk.reason})}else if(a==="tag"&&pk.type==="post"&&pk.tag){await f916.tag(secret,Number(pk.id),String(pk.tag).slice(0,40));counts.tag++;summary.actions.push({action:a,id:pk.id,reason:pk.reason})}else if(a==="comment"&&pk.comment&&counts.comment<2){const postId=Number(pk.post_id||pk.id),txt=String(pk.comment).slice(0,1600),res=await f916.comment(secret,postId,txt,null);counts.comment++;summary.actions.push({action:a,id:postId,reason:pk.reason,text:txt});conversations.push({direction:"out",type:"comment",id:res?.id??res?.comment_id??null,post_id:postId,author:"nomad17",topic_ru:String(pk.topic_ru||"").slice(0,100),source_text:txt,ru_translation:String(pk.comment_ru||"").slice(0,3000),simple_ru:String(pk.comment_simple_ru||pk.comment_ru||"").slice(0,3000)})}}catch(e){summary.notes.push(`${a} failed for ${pk.id}: ${e.message}`)}}const mu=decision.memory_update||{},mus=decision.memory_update_simple||{},now=new Date().toISOString();for(const k of["observations","hypotheses","questions","lessons"]){if(!Array.isArray(mu[k]))continue;for(const x of mu[k].slice(0,5)){const t=cleanText(x).slice(0,1000);if(t&&t!=="[object Object]")memory[k].push({at:now,text:t})}if(memory[k].length>200)memory[k]=memory[k].slice(-200)}applySocialMemory(memory,decision.social_memory_update||{},now);state.lastRun=now;await saveMemory(memory);await saveState(state);await audit({type:"cycle",summary});await appendJournal({at:now,mode:state.mode,citizens:p?.board?.citizens??null,label:mission?`Миссия: ${mission.slice(0,100)}`:(reads.length?`Прогулка: ${reads.length} интересных находок`:"Тихий цикл"),mission:mission||null,mission_report:decision.mission_report||null,daily_takeaways:(decision.daily_takeaways||[]).slice(0,3),selection_notes_simple_ru:String(decision.selection_notes_simple_ru||"").slice(0,1200),interests_snapshot:(memory.interests||[]).slice(0,8),open_loops_snapshot:(memory.open_loops||[]).slice(0,8),relationships_snapshot:Object.values(memory.relationships||{}).sort((a,b)=>(b.familiarity||0)-(a.familiarity||0)).slice(0,8),reads,hypotheses:(mu.hypotheses||[]).slice(0,5).map(cleanText),questions:(mu.questions||[]).slice(0,5).map(cleanText),lessons:(mu.lessons||[]).slice(0,5).map(cleanText),hypotheses_simple:simpleList(mus,"hypotheses",mu.hypotheses).map(cleanText),questions_simple:simpleList(mus,"questions",mu.questions).map(cleanText),lessons_simple:simpleList(mus,"lessons",mu.lessons).map(cleanText),actions:summary.actions,conversations});return summary}catch(e){summary.error=e.message;state.lastRun=new Date().toISOString();await saveState(state);await audit({type:"cycle_error",summary});throw e}}
